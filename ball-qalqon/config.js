// Публичный файл: секретов здесь нет и быть не должно — он уезжает на GitHub Pages.
//
// Почему адрес ручки замера прописан руками, а не подставляется deploy.sh, как
// у kvartira/toy: tools/deploy.sh для четырёх проверяемых продуктов просто копирует
// app/ как есть, ничего не генерируя. Значит файл должен быть рабочим прямо в репозитории,
// иначе страница уедет в прод с пустым API и тест молча не запишет ни одного события.
window.BQ_API = "https://jzsotdbrbmtiuxkptdme.supabase.co/functions/v1/mvp";

// Код продукта. На той стороне он в белом списке (ustoz|ball|avtomaktab|kontrakt),
// опечатка здесь = 400 на каждое событие и пустой разрез в отчёте.
window.BQ_PRODUCT = "ball";

// Цены. Держим числами, а не строками в вёрстке: каждая встречается на странице
// по нескольку раз, и разъехаться копиям — вопрос одной правки.
//
// ОТКУДА 99 000. Цена объясняется не нашими затратами, а тем, что человек уже
// собирается потратить. Лишение прав стоит повторного курса в автошколе:
// 5–8 млн сум, в среднем 7 млн (osonprava.uz, 22.03.2026) плюс полгода без
// машины. Пакет — примерно полтора процента от этой суммы. Второй ориентир —
// ближайший по продукту сосед: qalqonuz.com, узбекская платформа, которая
// собирает правовые документы автоматически, берёт 149 000 сум в МЕСЯЦ за тариф
// Pro (проверено 26.08.2026). Мы берём меньше и разово. Третий — цена ошибки
// на сроке: штраф за проезд на красный — 3 БРВ, то есть 1 236 000 сум при БРВ
// 412 000 (с 01.08.2025, указ УП-91, lex.uz/ru/docs/7553336) и 1 320 000 сум при
// БРВ 440 000 (с 01.09.2026, указ УП-115, lex.uz/ru/docs/8283680). Статья —
// 128⁴ ч. 2 КоАО, lex.uz/ru/docs/97661, открыто 27.08.2026. При оплате в первые
// 15 дней со дня вручения — вдвое меньше; пропущенное окно дороже пакета
// в шесть раз.
//
// ДВЕ ОГОВОРКИ, БЕЗ КОТОРЫХ ЭТОТ ДОВОД ЛОЖНЫЙ.
// Первая: скидки не бывает вовсе по ст. 131, 132, 136, 140 и ч. 3 ст. 211,
// а также при повторе того же состава в течение года (ст. 332¹ ч. 2 КоАО —
// три основания, а не одно; пять кругов подряд продукт называл только среднее).
// Там «пропущенное окно» стоит ноль, потому что окна нет. Сравнение приводится
// только там, где скидка есть, и на страницах это сказано вслух.
// Вторая: каждое число здесь названо парой «значение БРВ + дата, с которой оно
// верно» (Р-50), а не словом «сейчас». 1 сентября этот комментарий не устареет
// и править его не придётся — ни в этом году, ни при следующей смене БРВ.
//
// Число взято из кодекса, а не из обзора: обзор autostrada.uz от 27.02.2026
// по скорости отстал от ЗРУ-1033 на целую часть статьи, и один раз это уже
// стоило нам заниженной вдвое суммы на сайте. Значения БРВ раньше стояли
// со ссылкой на spot.uz — теперь на указы, потому что проверять нас человек
// пойдёт в lex.uz.
// 49 000 — та же работа по ОДНОМУ постановлению. Средний уровень нужен ради
// замера: человек с одним штрафом и человек с девятью баллами покупают разное,
// и при одной цене мы не узнаем, какое из двух.
window.BQ_PRICE = 99000;
window.BQ_PRICE_ONE = 49000;

// Порог SMS-предупреждения и порог лишения — ПКМ № 296 от 06.05.2025.
// Лежат здесь, а не в скрипте страницы, чтобы правились в одном месте вместе с ценой.
window.BQ_WARN = 8;
window.BQ_LIMIT = 12;
