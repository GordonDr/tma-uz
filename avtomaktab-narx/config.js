// Адрес общей ручки замера и код продукта.
//
// Файл публичный — он уезжает на Pages как есть, поэтому здесь нет и не может быть
// ключей. Писать в базу напрямую нельзя: анонимный ключ PostgREST отозван (Р-32),
// и правильно, что отозван. Единственный путь наружу — функция mvp, она работает
// под service_role на сервере.
//
// Адрес прописан руками, а не подставляется деплоем: tools/deploy.sh генерирует
// config.js только двум живым продуктам, а папки четырёх проверяемых копирует
// как есть. Значит файл должен быть самодостаточным.
window.MVP_API = "https://jzsotdbrbmtiuxkptdme.supabase.co/functions/v1/mvp";
window.MVP_PRODUCT = "avtomaktab";

// Цена пакета в одном месте: она стоит в карточке на странице и должна
// совпадать с тем, что человек выбирает в форме. Разъехавшиеся копии — то,
// ради чего в проекте появился claims.pl.
//
// Тариф один. Раньше их было два (29 000 «разбор» и 49 000 «разбор + чек-лист
// сравнения школ»), но чек-лист сравнения теперь работает прямо на странице
// и бесплатно — продавать его второй раз нельзя.
//
// Ключ называется paket, а машинное значение в форме осталось прежним —
// want = "redline". ПОЧЕМУ: по значениям want считается порог в PROVERKA.md §2,
// а он записан ДО посева. Переименовать значение сейчас — значит поменять
// правило подсчёта после того, как оно записано.
window.MVP_PRICES = { paket: 49000 };
