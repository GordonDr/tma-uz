// Адрес общей ручки замера и код продукта. Файл публичный — секретов здесь нет
// и быть не может: страница лежит на GitHub Pages, её исходник читает кто угодно.
//
// Почему не пишем в базу напрямую: анонимный ключ PostgREST в проекте отозван (Р-32),
// писать может только серверный код под service_role. Ручка mvp — единственный вход.
//
// Код продукта закрыт списком на сервере (ustoz | ball | avtomaktab | kontrakt):
// опечатка здесь не «потеряет» события, а вернёт 400 — это заметно сразу.
window.MVP = {
  EV:   "https://jzsotdbrbmtiuxkptdme.supabase.co/functions/v1/mvp/ev",
  LEAD: "https://jzsotdbrbmtiuxkptdme.supabase.co/functions/v1/mvp/lead",
  PRODUCT: "ustoz",

  // Цены показываем из одного места, чтобы текст на странице и цифра в заявке
  // не разъехались (Р-26: обещание — такой же факт, как значение в базе).
  // Цена одна и она разовая. PRICE_MONTH: 49000 отсюда УБРАНА: помесячного
  // тарифа не существует — tarif() знает два состояния, «full» и «bepul», и
  // нигде не сказано, что входит в помесячный вариант. Цена, за которой нет
  // описанного товара, обещает несуществующее.
  PRICE_FULL: 99000,   // «Imtihon toʻplami», разовая заявка

  // Ручное включение тарифа на время посева: касс нет (Р-07), первым платящим
  // владелец даёт ссылку с ?kalit=<это значение>.
  //
  // Это МЕТКА, а не замок, и притворяться иначе нельзя: значение лежит в файле,
  // который отдаётся всем. Настоящая проверка ставится на запись в базе (Р-22),
  // когда появится касса и появится что защищать — банк вопросов. Сегодня за
  // меткой стоит полный план: он строится в браузере из открытых данных,
  // и защищать его нечем в принципе.
  KALIT: "reja2026",
};
